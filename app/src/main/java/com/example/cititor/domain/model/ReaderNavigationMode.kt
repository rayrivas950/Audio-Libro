package com.example.cititor.domain.model

/**
 * Defines the navigation behavior of the reader.
 */
enum class ReaderNavigationMode {
    /**
     * Continuous vertical scrolling.
     */
    SCROLL,

    /**
     * Horizontal page passing (horizontal paging).
     */
    PAGINATED
}
